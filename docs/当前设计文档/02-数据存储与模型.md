# 02. 数据存储与模型

当前数据默认存放在 Vault 内 `project-manager-data/`。

```text
project-manager-data/
├── config.json
├── projects.json
├── project-pages.json
├── note-task-index.json
├── write-history.json
└── tasks/
    ├── 2026-05.json
    └── 2026-06.json
```

## 配置模型

`PluginConfig` 保存插件配置：

1. 视图名称：`overviewTab1Name`、`overviewTab2Name`、`dialogTabName`。
2. 数据目录：`dataFolder`。
3. 今日与排程默认值：`timeSlotMinutes`、`showCompletedTasks`、`defaultTaskDurationMinutes`、`defaultTaskStartTime`。
4. 快速记录写入位置：`dailyNoteFolder`、`dailyNoteDateFormat`、`dailyNoteMode`、`dailyNoteSingleFilePath`。
5. 快速记录默认目标：`defaultDialogTarget`。
6. Markdown 插入头：`noteAppendHeader`、`dailyNoteHeader`。

## 项目与项目页

`Project` 保存项目名称、描述、颜色、状态和时间戳。创建项目时同步创建一条 `ProgressPage`，用于保存项目页名称和表格列顺序。

删除项目会删除对应项目页和该项目下的任务。项目页排序保存在 `project-pages.json`。

## 任务模型

`Task` 是唯一真实任务记录：

```ts
type Task = {
  id: string;
  kind: "simple" | "composite";
  title: string;
  description?: string;
  projectId?: string;
  status: "todo" | "doing" | "blocked" | "done";
  priority?: "low" | "medium" | "high" | "urgent";
  tags: string[];
  date: string;
  startTime?: string;
  endTime?: string;
  recurrence: "daily" | "weekly" | "monthly";
  recurrenceCount?: number | null;
  recurrenceUntil?: string | null;
  consumeRequiresCompletion: boolean;
  occurrenceDates: string[];
  occurrenceStates: TaskOccurrenceState[];
  occurrenceOverrides: TaskOccurrenceOverride[];
  subtasks: TaskSubtask[];
  viewState: TaskViewState;
  sourceLinks: TaskSourceLink[];
  notes: TaskNote[];
  mindmapComments: TaskMindmapComment[];
  createdAt: string;
  updatedAt: string;
  revision: number;
};
```

`occurrenceDates` 是任务系列的日期种子和已落盘实例日期集合。页面层不会直接把它等同为“已执行次数”，而是通过领域规则把一条 `Task` 展开为有效的 `TaskOccurrence`。

时间字段只接受 `HH:mm`，取值范围为 `00:00` 至 `23:59`。开始时间和结束时间必须同时填写，且结束时间必须晚于开始时间；不符合格式的输入会在 `ProjectManagementStore` 写入入口被拒绝。

## 重复规则

核心重复类型只有三种：

1. `daily`：每日此时重复。
2. `weekly`：每周此时重复。
3. `monthly`：每月此时重复。

单次任务不是独立类型，而是 `daily + recurrenceCount: 1` 的快捷选择。永久重复不是独立类型，而是 `daily + recurrenceCount: 2147483647` 的快捷选择；为避免一次写入过多实例，实际生成的日期集合有防爆上限。

`consumeRequiresCompletion` 对应“执行足额次数”：

1. 开启时，只有已完成实例会消耗执行次数。
2. 开启后，若原计划日期已过但任务未完成，任务会在当前有效日期继续展示，剩余执行次数不减少。
3. 开启后，勾选完成才会写入 `TaskOccurrenceState` 并消耗 1 次；完成后再按重复类型计算下一次有效日期。
4. 关闭时，`doing` 状态实例无论是否完成都会消耗执行次数。
5. `todo` 和 `blocked` 状态的未完成实例不消耗执行次数。
6. 填写 `recurrenceUntil` 时，结束日期是硬边界，结束日期之后不再生成有效实例。

## 组合任务

组合任务是容器，不是可执行任务：

1. `kind: "composite"` 表示任务是组合容器。
2. 组合任务不支持状态、优先级、标签、重复次数和执行足额次数；写入时统一收敛为 `daily + recurrenceCount: 1`。
3. `subtasks` 不承载业务数据，子任务一律是独立 `Task`。
4. `viewState.mindmap.parentTaskId` 指向某个组合任务时，当前普通任务是挂入该组合任务的子任务。
5. 子任务与普通任务等价，保留自己的状态、重复规则、完成状态、来源链接、笔记和视图状态。

组合任务的日期和时间范围由挂入子任务决定；空容器在甘特图中的范围为 0。

## 完成状态与单次覆盖

`TaskOccurrenceState` 只保存已完成实例。普通任务只要存在对应日期状态即为完成；开启“执行足额次数”的普通任务以完成状态数量作为已执行次数，未完成的过期日期不会计入已执行。组合任务本身不保存执行进度，挂入组合任务的子任务使用自己的 `TaskOccurrenceState` 计算进度，再由视图层汇总到组合父任务展示。

`TaskOccurrenceOverride` 保存重复任务某一次实例的标题、描述、开始时间、结束时间、跳过标记和原因。展开实例时优先使用覆盖字段。

## 视图状态

`TaskViewState` 保存表格以外的视图状态：

1. `board`：看板列和排序。
2. `gantt`：甘特图排序、依赖、锁定和里程碑。
3. `mindmap`：思维导图父级、排序、展开和坐标。

思维导图评语保存在 `mindmapComments`，可以挂在任务节点下，也可以挂在另一条评语下。普通任务和子任务都可以补充导图评语。

## 写入规则

所有业务写入都经过 `ProjectManagementStore`。新增、编辑、删除、完成状态变化会写入对应 `tasks/YYYY-MM.json`，然后回读磁盘并触发视图刷新。写入历史保存在 `write-history.json`。
