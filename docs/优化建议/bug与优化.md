
# 执行足额次数：bug
      我任务设置了重复5次，执行足额次数选择了 true，5天没执行（任务没有进入已完成名单），5天后任务咋就没了？？（不应该没有，应该还有剩余5次执行，例如：在今日任务页 点击了任务 ，任务进入了 已完成名单，此时可以算 任务执行了1此，剩余4此）。
      没执行，就不能算执行足额次数，应该算0次，剩余5次
 
# 修改建议 和 行动指南 （最重要）
   【1】分析上述bug 和 优化建议 之间的关系（彼此之间是有关系的），给出一个整体的修改方案
   【2】优先选择重构为最合理的设计方案，禁止兼容旧版本考虑
   【3】代码必须采用模块化设计，每个模块都有自己的功能，互不干扰
         适当新建文件夹 和 新建文件，将代码分类存放
   【4】完成后更新文档./docs
         (禁止写无关内容，禁止写旧版本设计，只需保证 文档与代码 对应即可)
